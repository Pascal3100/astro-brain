# Archive journal — Revue d'architecture et fiabilisation avant reprise du fil matériel (Sessions 48→50)

Milestone **qualité de code** intercalé entre la tranche Oracle (S43→47) et la reprise du fil matériel Macro 3 (S51→53). Trois sessions qui ne livrent aucune feature et assainissent le terrain : revue solution-architect du backend exécutée en subagent-driven-development (S48), fiabilisation de l'app côté offline/catalogue (S49), puis mise à jour du Pi avec 133 commits de retard et correction de ce que le déploiement a révélé (S50). Sessions 48→50, août 2026. Se termine sur backend et app verts, backlog ruff vidé, Pi à jour — état de départ du diagnostic de la liaison monture.

---

**Session 48 — Revue d'architecture backend + refacto (branche `refacto/archi-review`)** : revue solution-architect du backend Pi (+ nettoyage app) figée en plan, exécutée en subagent-driven-development (9 tâches du plan + 2 hors-plan autorisées). Livré (13 commits, +718/−481, 53 fichiers) :
- **Code mort supprimé** : helper heading + module renommé (T1), table `mount_limits` via migration forward `_005` (T2), modèles/méthodes app Flutter morts (T3), 2 helpers INDI `set_number_values`/`indi_state_string` (T10-A).
- **Correctness / archi** : lecture I2C LIS3MDL hors event-loop (`asyncio.to_thread`, T4), helper `_publish_error` factorisé sur ~12 sites (T5), résidu de calibration **sigma live** + bascule sémantique « RÉSIDU » MAD→RMS (T6, **changement visible UI**), itérateur bus partagé `iter_state_snapshots` consommé par les 3 réacteurs gardés séparés (T7), fix GPS exposé via source typée `GpsSource.latest_fix()` hors bus santé (T8).
- **Alignement persistant (A1)** : `is_aligned` réhydraté au boot depuis le modèle SQLite (`alignment_repo.load`, garde-fous 12 h/20 m enfin câblés), déclencheur one-shot gaté sur fix **3D**, `AlignmentInvalidator` conservé pour la perte driver (T9). **Limitation acceptée** (ADR 2026-08-11) : monture power-cyclée backend éteint → faux positif « aligné » possible, non détectable sans introspection driver.
- **Backlog ruff** vidé (0 violation) dont conversion 5 enums → `StrEnum` behavior-preserving (T10-B).
- Suites vertes : **backend 307**, **app 262** + `flutter analyze` clean. Revue globale opus : **APPROVED**, zéro Critical/Important. ADRs 2026-08-11 (GpsFix typé + is_aligned SQLite).

**Session 49 — Debug UX app + refonte filtres Catalogue (branche `refacto/splash-offline-nominal`, mergée `main`)** : détour hors fil Oracle pour fiabiliser l'app côté offline/catalogue. Livré (3 commits, fast-forward sur `main` + push origin) :
- **Splash** : suppression de la page-barrage « astrobrain injoignable » — Pi injoignable = boot **nominal** (Oracle hors ligne), on entre en offline silencieusement ; machinerie d'échec morte retirée (state/screen/cubit).
- **Almanach** : la synchro ne partait pas au lancement — cause racine **environnementale** (repo GitHub privé → assets de la release `almanac-latest` en 404 pour un client anonyme), résolue en passant le repo **public** (scan sécurité préalable : aucun secret tracké, `secrets.h` bien gitignoré). Zéro changement code.
- **Catalogue** : retrait du pré-filtre Messier (chip + chaîne event/bloc/state/repo/provider) ; le champ **donnée** `messier` conservé (badge + recherche texte).
- **Catalogue** : chips `MAG ≤ 3/2` → **RangeSlider** magnitude (bornes −5→+12, pas de 1, requête au relâchement). **Option B** : les DSO sans magnitude cataloguée restent toujours visibles ; seuls les objets notés sont bornés à [min, max] (fixed + ephemeris providers).
- Validé sur device (Moto g54, APK release, Pi éteint). Suites : **app 261** + `flutter analyze` clean.
- Reste : observabilité de `almanacSync.sync()` (résultat encore silencieusement ignoré) → backlog.

**Session 50 — Déploiement Pi + 4 correctifs d'observabilité/correctness (`main`, 2 commits)** : mise à jour du Pi avec la base de code S43→S49 (133 commits de retard), puis correction de ce que le déploiement a révélé. Pas de monture branchée (les erreurs de connexion driver sont nominales).
- **Déploiement** : Pi `fa42e52` → `b86c755`, migrations `_004`+`_005` appliquées (`schema_version` 3 → 5), `catalog_objects` et `mount_limits` confirmées droppées. **`reference.sqlite` téléchargée au boot (13,8 Mo)** — 1ʳᵉ validation end-to-end de la chaîne Oracle SP2 sur le vrai Pi (`/reference/status` `ready: true`). `state.db` sauvegardée avant chaque pull.
- **GPS no-fix traité comme une erreur (correctness, pas juste du bruit)** : `gpsd-py3` lève `UserWarning('GPS not active')` dès que le démon rapporte `mode < 1` — état **nominal** sans fix. Le `except Exception` de `GpsdAdapter._loop` sautait tout le corps de boucle : à la **perte** du fix, l'état publié restait figé sur le dernier fix et `latest_fix()` continuait de servir une **position périmée** à l'orchestrateur et au bridge d'alignement (risque d'aligner/GoTo sur une position morte). Effet visible par ailleurs : **4121 lignes de journal en 2 min 30** (une stacktrace par poll, 2/s, sur la carte SD). Le no-fix suit désormais le chemin de publication normal ; logs **par transition** d'état, erreurs réelles une fois par épisode (ré-armées au rétablissement). Boucle mise sous tests (elle n'en avait aucun).
- **Root logger jamais configuré** : découvert en vérifiant le correctif suivant sur le Pi — uvicorn ne configure que ses propres loggers, le root restait à `WARNING`, donc **tous** les `logger.info` du package étaient jetés en prod (seules les stacktraces WARNING passaient — ce qui explique qu'on n'ait jamais vu que le bruit). `configure_logging()` dans `main.py`, niveau réglable par `ASTRO_BRAIN_LOG_LEVEL`.
- **Sync `reference.sqlite` muette au boot** : succès non logués, et `create_task` nu perdant toute exception hors du set géré (`HTTPError`/`KeyError`/`ValueError`). INFO sur les issues nominales + wrapper `_boot_reference_sync` pour que l'inattendu (disque plein, cache illisible) atteigne journald.
- **Migration `_006`** : purge les lignes `calibration_sensor` des 2× ADXL345 retirés le 2026-07-17, jamais relues (`SENSOR_IDS` = `{lis3mdl}`) et encore présentes sur le Pi. Vérifié après redéploiement : `schema_version` 6, table vide.
- **Ops** : symlink `/usr/local/bin/uv` (le PATH d'un `ssh pi '<cmd>'` non interactif ignore `~/.local/bin`, et l'early-return de `~/.bashrc` empêche tout export d'agir) + procédure de sauvegarde `state.db` documentées dans [`deployment.md`](../technical/deployment.md).
- **Vérifié sur le Pi après redéploiement** : **11 lignes de journal en 60 s** (contre 4121 en 2 min 30), zéro `GPS not active`, et les lignes utiles enfin visibles (`reference: déjà à jour (schema v2)`, `gps state: - -> no_fix (sats=0)`, connexion INDI). Suites : **backend 317** + ruff clean.
- **Aucune calibration compass persistée** sur le Pi (`calibration_sensor` vide) : la calibration LIS3MDL n'a jamais été refaite depuis le passage en heading non tilt-compensé (S42). À faire lors d'une session terrain.
- **Deux points de finition traités dans la même session** (3ᵉ commit) : `httpx`/`httpcore` réduits à `WARNING` sauf en `DEBUG` (ils loguaient l'URL signée complète de la release Oracle, ~700 caractères par requête) + confirmation du niveau effectif au démarrage ; et le **test flaky pré-existant** `test_round_trip_start_finalize_status` corrigé — le `await asyncio.sleep(0.7)` fixe qui coursait l'accumulation de 500 échantillons est remplacé par une attente du **flux public de progression** bornée par `asyncio.timeout`. Au passage, le premier jet de `configure_logging()` n'était pas idempotent (passer à `DEBUG` après un appel en `INFO` laissait `httpx` bloqué à `WARNING`) : la fonction est désormais déclarative. Suites : **backend 319** sur 3 exécutions complètes consécutives, ruff clean.
- **Smoke test de toute la surface REST/SSE sur le Pi** (banc `smoke.py`, 32 contrôles avec attendu déclaré) : **30/32 conformes**. Ce qui est prouvé sans monture : résolution Oracle des 14 363 objets (`NGC205`, `planet:mars`, `sun` atteignent tous `409 not_aligned` — donc le resolver passe — vs `404 unknown_id` sur un id bidon), alignement sans fix GPS (`409 position requise` → `POST /align/location/client` → étoiles visibles avec az/alt, Sirius à 29,5°), session start/get/delete, carte de constellation orientée, 8/8 contrats d'erreur exacts, SSE `/events`, `POST /reference/sync` → `up_to_date`. Non testable sans monture : slew/GoTo réels, `record`/`finalize` d'alignement (délibérément non forcés — `finalize` persisterait un modèle bidon rendant `is_aligned` vrai au boot).
- **Par conception, vérifié comme tel** : `POST /tracking` et `POST /stop` renvoient `200 ok:true` alors qu'INDI lève `KeyError: 'TRACK_ON'`/`'ABORT'` (sans monture le driver n'expose pas ces switches). C'est `_publish_error` — log + publication sur la bus, la route reste `ok` — cohérent avec l'ADR « REST pour les commandes, SSE pour l'état » : `GET /state` juste après montre bien `mount=error ('TRACK_ON')`.
- **Le seul vrai défaut trouvé, corrigé (4ᵉ commit)** : capteur I2C absent → `500` sur `POST /calibration/lis3mdl/start` et **crash ASGI** (corps chunké tronqué) sur `GET /sensors/compass/stream`. Sondage du bus : **aucune adresse ne répond sur `i2c-1`, le LIS3MDL est physiquement absent** — même classe de bug que le GPS no-fix : un état matériel **nominal** rendu comme une panne serveur. L'`OSError` de smbus2 est traduite en `SensorUnavailableError` (domaine) → `503`. Défaut structurel au passage : `_LazySensor.__aenter__` appelait `adapter.start()` *depuis le générateur*, donc après l'envoi du `200` — une panne ne pouvait plus devenir un statut HTTP. Le capteur est désormais acquis **avant** de rendre la réponse (`acquire()`/`release()` explicites), et une puce perdue en vol émet un événement SSE `error` avant fermeture. Suites : **backend 324**, ruff clean. Redéployé et vérifié sur le Pi (`2c444b6`) : boot en 10 lignes, **smoke test 32/32** (les deux contrôles capteur attendent désormais `202` *ou* `503 absent`), et le journal montre `503 Service Unavailable` dans le log d'accès — zéro stacktrace, zéro exception ASGI.
- **LIS3MDL physiquement absent du bus** : aucune adresse ne répond sur `i2c-1`. Le backend le dit maintenant proprement (`503`), mais le compass reste inutilisable jusqu'à une revue du câblage — fil matériel à ouvrir, pas un correctif logiciel. Explique aussi l'absence de calibration persistée.
- **Power-save WiFi : le Pi paraît éteint alors qu'il tourne.** Le `git pull` de fin de session a échoué sur `No route to host`, et un balayage ICMP du `/24` ne voyait pas le Pi — alors qu'il était up depuis 2 h 20, à 53,7 °C, `throttled=0x0`, signal −52 dBm. C'est `nmap` (SYN TCP répété) qui l'a réveillé, puis tout a fonctionné. Cause : `Power save: on` sur `wlan0` → les ARP broadcast sont ratés au repos, donc tout client au cache ARP froid échoue. **Leçon de méthode** : `ping` négatif ne prouve pas « hôte absent » sur un Pi en WiFi. Correctif proposé (drop-in NetworkManager global) → [`backlog.md`](backlog.md), en attente de validation.

